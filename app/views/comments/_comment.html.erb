# # <% unless comment.parent_id || !comment.persisted? %>
#   <div class="card">
#     <div class="card-body">
#       <%= comment.comment %>
#       <%= link_to 'reply', new_article_comment_path(@article, parent_id: comment.id), remote: true, class: 'd-block' %>
#       </div>
#   </div>
#   <% if comment.replies.any? %>
#     <% comment.replies.each do |reply| %> 
#       <%= render partial: 'comments/reply', locals: { reply: reply } %>
#     <% end %>
#   <% end %>
#   <div id="reply-form-<%= comment.id %>"></div>
# <% end %>




<%= comment.comment %>
      <%= link_to 'reply', new_article_comment_path(@article, parent_id: comment.id), remote: true, class: 'd-block' %>
      </div>
  </div>
  <% if comment.replies.any? %>
    <% comment.replies.each do |reply| %> 
      <%= render partial: 'comments/reply', locals: { reply: reply } %>
    <% end %>
  <% end %>

  <div id="reply-form-<%= comment.id %>"></div>